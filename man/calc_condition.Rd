% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_condition.R
\name{calc_condition}
\alias{calc_condition}
\title{Calculate species condition}
\usage{
calc_condition(
  channel,
  surveyData,
  lengthweight,
  aggregate_by,
  by_sex = FALSE,
  length_break = NULL
)
}
\arguments{
\item{surveyData}{Data table. NEFSC survey data generated by \code{get_survdat_data.R}}

\item{lengthweight}{Data table. Length-weight parameters. Options = "survdat" or "Wigley".}

\item{aggregate_by}{vector. Spatial scale to aggregate by: "EPU", "strata", or "none".}

\item{by_sex}{logical. If TRUE, calculates condition by sex. If FALSE, calculates condition across sexes.}

\item{length_break}{numeric vector. If not NULL, will calculate condition by length breaks specified in the vector. User must specify minimum and maximum lengths in this parameter, e.g., c(0, 20, 70). If NULL, will not calculate by length groupings.}
}
\description{
This function calculates species condition from NEFSC survey data 'survdat::get_survdat_data(channel, getBio = TRUE, getLengths = TRUE)'
Methods derived from Laurel Smith (https://github.com/Laurels1/Condition/blob/master/R/RelConditionEPU.R)
}
\details{
#' @param channel an Object inherited from \link[DBI]{DBIConnection-class}.
   This object is used to communicate with the database engine.
   (see \code{\link[dbutils]{connect_to_database}})
}
\examples{

\dontrun{
# Pull data and apply conversion corrections
data <- get_survdat_data(channel)
# Calculate condition aggregated by EPU
calc_condition(channel = channel, surveyData=data$survdat, lengthweight = "Wigley", aggregate_by = "EPU", by_sex = FALSE, length_break = NULL)
}

}
